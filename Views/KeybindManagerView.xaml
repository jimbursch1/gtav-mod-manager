<UserControl x:Class="GtavModManager.Views.KeybindManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GtavModManager.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Filter bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
            <TextBox Style="{StaticResource TextBoxStyle}" Width="220"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0" />
            <Button Style="{StaticResource ButtonStyle}" Content="Rescan"
                    Command="{Binding RunConflictScanCommand}" />
        </StackPanel>

        <!-- Keybind table -->
        <DataGrid Grid.Row="1"
                  Style="{StaticResource DataGridStyle}"
                  ItemsSource="{Binding FilteredKeybinds}"
                  Margin="8,4,8,8">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasConflict}" Value="True">
                            <Setter Property="Background" Value="#22EF5350" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mod" Binding="{Binding ModName}" Width="160" />
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="*" />
                <DataGridTextColumn Header="Key" Binding="{Binding DisplayKey}" Width="100" />
                <DataGridTextColumn Header="Config File" Binding="{Binding ConfigFile}" Width="200" />
                <DataGridTextColumn Header="Format" Binding="{Binding Format}" Width="60" />
                <DataGridTemplateColumn Header="⚠" Width="24">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="⚠" Foreground="{StaticResource ErrorBrush}"
                                       Visibility="{Binding HasConflict, Converter={StaticResource BoolToVis}}"
                                       HorizontalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit" FontSize="11" Padding="4,1"
                                    Command="{Binding DataContext.EditKeybindCommand,
                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
